1、在一般的调试器中，F7表示单步步进，F8表示单步步过，在一般指令执行时没有区别，但是call指令如果用F7，就是进入到call指令指向的内存单元，
然后逐条执行指令，而F8则是一次性执行完call指向的内存单元的指令。
2、F7的原理是设置EFL标志寄存器的TF位为1,因为如果TF位为1，则cpu进行单步执行状态。
3、F2是下断点，原理就是把目标断点处的值设为cc，即指令int 3，cpu碰到int 3指令，就会知道这是一个断点，执行到这里就会暂停。
4、反调试的目的就是让破解者单步调试失效，迷惑破解者，让他不能了解程序的真正执行过程